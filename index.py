a []

a.append({'nome': 'bianca', 'date': datetime.datetime.now(), 'tipo': 'N', 'atendido': False, 'posicao': len(a)})

def getPessoa(posicao):
    pessoa = None
    for i in a:
        ifi['posicao'] == posicao:
            pessoa = i 
        return pessoa
    for i in range(index-1, len())

getPessoa()
    for i in range(index-1, len())

print(getPessoa(0))
